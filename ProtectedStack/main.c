#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <assert.h>

#include "stack.c"
#include "break.c"

int main() {

    /*
    stack_t* myStack = newStack (3);

    stackPush (myStack, 5);
    stackPush (myStack, 9);
    stackPush (myStack, -2);
    stackPush (myStack, 3);

    stackDump (myStack);

    printf("%d\n", stackPop(myStack));
    printf("%d\n", stackPop(myStack));
    printf("%d\n", stackTop(myStack));
    printf("%d\n", stackPop(myStack));
    printf("%d\n", stackPop(myStack));

    stackDump (myStack);

    */

    breakStack();

}




